interface:
  display_name: "Lane Orchestrate"
  short_description: "Run, compare, and promote Lane-backed agent attempts."
  default_prompt: "Use Lane to run multiple isolated implementation attempts, verify them, compare the evidence, promote the selected work, and clean up the losing lanes."
